@model List<QueryRunner.Models.ViewStudentMarks>

@{
	/**/

	ViewBag.Title = "ListStudents";
	Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>View Students</h2>
<table class="table">
	<tr>
		<th>
			Username
		</th>
		<th>

		</th>
	</tr>
	@foreach (var item in Model)
	{
		<tr>
			<td>
				@Html.DisplayFor(modelItem => item.Name)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.Presentage)
			</td>
		</tr>
	}
	<tr>
		<td>
			@Html.ActionLink("Export Marks", "exportAllStudentMarks", "Student")
			@*<md-button id="getMarks" class="btnBlue" onclick="getMarks()">Save marks to textfile</md-button>*@
		</td>
	</tr>
</table>


@*<script>

	getMarks = function () {
		if (@Model.Count == 0) {
			var ID = -1;
		}
		else {
			var ID = @Model.First().TestID;
		}
					$.ajax({
						url: '@Url.Action("exportAllStudentMarks", "Student")',
											dataType: "json",
						type: "GET",
						contentType: 'application/json; charset=utf-8', //define a contentType of your request
						cache: false,
						data: { ID: ID },
						success: function (data) {
							// data is your result from controller
							if (data.success) {
								alert(data.message);
							}
						},
						error: function (xhr) {
							alert('Something went wrong!');
						}
					});
	}
</script>*@
